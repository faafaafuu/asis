<div align="center">

# Суфлёр

**Выделите непонятное слово — и рядом появится объяснение.**
Или просто позовите по имени: спросите голосом, попросите напомнить о деле.
В любой программе, а не только в браузере.

<img src="docs/demo-select.gif" width="720" alt="Выделенное слово «альбедо», рядом всплывает окно с определением, затем раскрываются «простыми словами» и примеры">

[**Скачать**](../../releases) · [**Посмотреть вживую**](http://84.247.166.53:5173/demo.html) · [Как пользоваться](#как-пользоваться) · [Настройки](#настройки)

</div>

---

## Что это

Небольшая программа, которая живёт в трее и ждёт. Встретили в статье, письме или коде
незнакомый термин — выделили его с зажатым **левым Ctrl**, и рядом с текстом всплыло
короткое объяснение. Не нужно переключаться в браузер, открывать вкладку и терять место,
на котором читали.

- **Работает поверх любого приложения** — браузер, почта, PDF, редактор кода, мессенджер.
- **Отзывается на имя.** Скажите «Ноа» — и можно спрашивать голосом, не трогая клавиатуру.
- **Объясняет по сути.** Сначала одно предложение. Мало — нажмите «?», и появятся
  «простыми словами» и примеры.
- **Можно доспросить.** Прямо в окне: «а почему так?» — с учётом исходного термина.
- **Помнит дела.** «Ноа, напомни завтра в десять позвонить в банк» — и задача
  появится в списке со сроком, без единой клавиши.
- **Не мешает.** Обычное выделение текста ничего не открывает. Только с левым Ctrl.
- **Не забирает фокус.** Курсор остаётся там, где был, выделение не сбрасывается.
- **Светлая и тёмная тема** — по настройке системы.

## Как пользоваться

| Действие | Что происходит |
|---|---|
| Выделить текст с зажатым **левым Ctrl** | открывается объяснение |
| Нажать **?** | «простыми словами» и примеры |
| Написать в поле «Спросить ещё…» | уточняющий вопрос по этому же термину |
| **Esc** | закрыть |
| Тянуть за заголовок или за край | подвинуть или растянуть окно |
| Сказать **«Ноа»** | помощник начинает слушать |
| **Пробел** в открытом окне | прочитать вслух |
| **Левый Alt + Пробел** | спросить голосом, удерживая клавиши |
| **Ctrl + Shift + Alt + Пробел** | включить или выключить ожидание имени |
| Сказать **«Ноа, напомни …»** | завести дело со сроком |
| Сказать **«Ноа, какие у меня дела»** | услышать список |

### Окно

Закрывается по **Esc**. Щелчок мимо окна его не закрывает; забытое окно уходит
само через минуту — минута отсчитывается от последнего события, поэтому во время
чтения, движения мыши или ответа вслух окно остаётся.

Окно двигается за заголовок и растягивается за края и углы. Пока размер выбран
вручную, окно не подгоняется под содержимое; следующий вопрос открывает его как
обычно.

На вопрос, заданный голосом, окно открывается посреди экрана, чуть выше середины.

На телефоне иначе: выделите текст обычным способом и выберите **«Объяснить»** в меню
рядом с «Копировать».

<img src="docs/menu.png" width="300" alt="Меню выделения на телефоне: Копировать и Объяснить">

## Установка

Зайдите в [**Releases**](../../releases), скачайте `.exe` и запустите.

Пока программа выпускается только под **Windows 10 / 11**. Код для macOS, Linux
и Android в репозитории есть, но их сборки либо ни разу не собрались, либо ни разу
не были проверены в работе, — а обещать файл, которого нет, хуже, чем честно его
не предлагать. Как заработают, появятся в releases.

### Первый запуск

Появится синее окно «Windows защитила ваш компьютер» → «Подробнее» →
«Выполнить в любом случае». Так система встречает любую программу без платной
подписи разработчика.

Если Defender молча удалил файл — это ложное срабатывание на неподписанный
установщик. Сверьте SHA-256 скачанного файла со значением в описании релиза
или сообщите о нём через
[Microsoft Security Intelligence](https://www.microsoft.com/en-us/wdsi/filesubmission).

При запуске открывается окно настройки — в нём живая проверка перехвата: выделите любое
слово с левым Ctrl, и строка покажет, дошёл ли жест и удалось ли получить текст. Окно
можно закрыть сразу, программа продолжит работать в трее. Повторный запуск ярлыка не
плодит копии, а снова показывает это же окно.

Дальше вмешиваться не нужно. Программа прописывается в автозапуск и при следующем
входе в систему поднимается сама — молча, без окна, сразу в трей. Там же она запускает
Ollama, если та не отвечает, и заранее загружает модель в память, чтобы первое
выделение не ждало загрузки. Автозапуск отключается галочкой «Запускать вместе
с системой» в том же окне.

В первый раз модель ещё нужно скачать — несколько гигабайт, и до конца загрузки
объяснения приходить не будут. Какая именно, программа решает сама по видеопамяти:
на карте от 8 ГБ — `qwen2.5:7b`, от 6 ГБ — `gemma3:4b`, без видеокарты — маленькая
`gemma3:1b`, потому что считать будет процессор.

### Удаление

Обычным способом: Параметры → Приложения → Sufler → Удалить. Программа живёт в трее и
сама себя закрывать не обязана, поэтому деинсталлятор снимает её перед удалением файлов.
Если удаление всё же встало — снимите `sufler.exe` в Диспетчере задач и повторите.

## Голос

<div align="center">

<img src="docs/demo-voice.gif" width="720" alt="Индикатор помощника: слушает, думает, отвечает — и рядом появляется ответ на заданный голосом вопрос">

</div>

Скажите **«Ноа»** — и помощник начнёт слушать. Можно сразу с вопросом: «Ноа, что
такое альбедо». Ни клавиш, ни окна для этого не нужно — он ждёт имени всё время,
пока запущен.

Имя узнаётся на слух, а не по написанию: расшифровка пишет его то «Ноя», то
«Ной», то «Нова», и одна буква расхождения помощника не смущает. Слова, похожие
на имя, считаются обращением только когда за ними пауза, — «ночь была тихая»
разговор не начинает.

**Ctrl + Shift + Alt + Пробел** включает и выключает это ожидание, не открывая
настройки. Выключенным оно не занимает ни микрофон, ни видеопамять: распознавание
останавливается целиком, а это под полтора гигабайта. Галочка «Отзываться на имя»
в окне настройки делает то же самое.

Пробел в открытом окне — прочитать объяснение вслух. Читается то, что появилось
последним: открыли — определение, нажали «?» — «простыми словами» и примеры,
спросили — ответ.

Левый Alt с пробелом — задать вопрос голосом: держите клавиши и говорите,
отпустите — вопрос уйдёт в тред, а ответ прозвучит вслух. Это работает и при
закрытом окне: спросите с чистого места, и окно откроется само уже с ответом.

После первого ответа начинается разговор: клавиши больше не нужны, просто
говорите. Программа слушает, отвечает вслух и снова слушает — пока вы с ней не
попрощаетесь: «спасибо», «до связи», «ну всё, пока», «давай прощаться». Коротко
«хватит», «стоп», «замолчи», «отбой» — тоже конец разговора, если фраза ими и
исчерпывается: «стоп, подожди, я про другое» разговор не обрывает. Разговор
заканчивается, окно закрывается вместе с ним.

**Esc** останавливает всё, что голос делает в эту секунду: речь, слушание,
«думаю» — с окном и без. Ответ, который ещё думался, после Esc не прозвучит, а
фраза, расшифрованная уже после нажатия, не выполнится. Заказ, остановленный
посреди сборки корзины, не оплачивается — оплатить его можно кнопкой в окне
заказа. Ожидание имени после Esc остаётся: остановлено текущее, а не помощник.

Пока программа говорит, микрофон не слушает: иначе она записала бы собственный
голос из колонок и ответила бы сама себе. Если перебить её левым Alt с пробелом,
она замолчит и начнёт слушать.

Голос и распознавание работают на этом же компьютере и скачиваются из окна
настройки: синтезатор с голосом — около 80 МБ, распознавание — 1,5 ГБ
(2,1 ГБ со сборкой под видеокарту NVIDIA, зато расшифровка почти мгновенная).
Скорость речи настраивается ползунком.

Пока окно открыто, пробел принадлежит ему и в другие программы не попадает —
иначе он печатался бы в том тексте, который вы читаете. Закройте окно клавишей
Esc, и пробел снова обычный. Внутри самого окна — например в поле «Спросить
ещё…» — пробел работает как обычно.

Сочетание левый Alt с пробелом занято всё время, пока программа запущена, а не
только при открытом окне: иначе им нельзя было бы задать вопрос с чистого места.
В Windows это сочетание открывает системное меню окна — с Суфлёром оно
открываться перестанет.

## Дела

Скажите **«Ноа, напомни завтра в десять позвонить в банк»** — или впишите дело
прямо в окно списка задач. Понимает не заранее заготовленные фразы, а смысл:
«добавь на сегодня доделать резюме», «отметь резюме сделанным», «перенеси банк
на завтра», «какие у меня дела на сегодня» — каждую реплику разбирает модель,
поэтому формулировка может быть любой.

Список делится на группы — **Просрочено**, **Сегодня**, **Дальше**,
**Когда-нибудь** — и хранится рядом с настройками, на диске у вас, а не на
сервере. Дело можно попросить разложить на шаги: «помоги с резюме» — и вместо
одной строки появится подсказка, с чего начать.

Дела со сроком можно дополнительно отправлять в Google Календарь — по своему
ключу, который заводится в окне настройки; без ключа список остаётся только
внутри программы. Если отправка не удалась, дело всё равно остаётся в списке.

## Компьютер

Ноа управляет компьютером голосом — теми же словами, что вы сказали бы
человеку рядом.

- **Программы и окна.** «Открой телеграм», «закрой хром», «запусти киберпанк»,
  «запусти мортал шелл в песочнице», «открой диспетчер устройств», «открой
  параметры блютуз». Программы ищутся в меню «Пуск», в Steam и в папках с
  играми, и название можно сказать как угодно — «клауде», «стим», «мортал шелл
  два».
- **Сайты.** «Открой вайлдберриз», а следом «давай посмотрим чехлы для айфона» —
  откроется выдача самого сайта в вашем браузере, с вашим входом и корзиной.
  «Пролистай вниз», «назад», «закрой вкладку» — по открытой странице.
- **Вопросы к интернету.** «До скольки работает кафе на Сивцевом Вражке» — Ноа
  ищет в поисковике и отвечает голосом по найденному, называя, откуда ответ.
- **VPN.** «Включи впн», «выключи впн» — туннель установленного VPN-клиента.
- **Питание.** «Усыпи компьютер», «заблокируй экран» — сразу после ответа.
  «Выключи компьютер», «перезагрузи» — через минуту; «отмени выключение»
  отменяет.
- **Состояние.** «Что грузит компьютер», «сколько места на диске», «кто занимает
  память» — загрузка процессора, самые тяжёлые программы, свободное место на
  дисках. Цифры берутся у самой Windows, модель их не пересказывает.
- **Ошибки.** Выскочила ошибка — «Ноа, что это за ошибка?». Ноа читает окно,
  которое было впереди, и ошибки журнала Windows за последние полчаса,
  объясняет, что случилось, и говорит, что сделать. Если по этим данным причина
  не видна, так и скажет — и назовёт, что проверить.
- **Файлы.** «Найди на компьютере фото паспорта», «где мой договор» — поиск по
  индексу Windows и по папкам «Рабочий стол», «Документы», «Загрузки»,
  «Изображения» и OneDrive. Один найденный файл открывается выделенным в своей
  папке, несколько — списком в проводнике. Ищется по имени файла: снимки с
  телефона названы датой, и тогда Ноа открывает «Изображения».
- **Печать и сообщения.** «Напечатай: буду через десять минут» — текст
  впечатывается в окно, где вы работаете. «Напиши Маше в телеграм, что я
  опоздаю» — Ноа откроет Telegram и запомнит текст; откройте нужный чат и
  скажите «вставь». Отправляет Ноа только после «да» на вопрос «Отправить?»:
  сообщение, ушедшее по ошибке распознавания, не вернуть.

## Заказ продуктов

«Ноа, закажи яйца, хлеб и молоко» — продукты подбираются голосом по настоящим
ценам, и всегда в одном магазине. Цены сравниваются во **ВкусВилле**,
**Магните**, **Метро** и **Пятёрочке** — последняя по ключу parse.bot. Назовёте
магазин — подбор пойдёт там; не назовёте — там, где полнее и дешевле.

Корзину Ноа собирает во ВкусВилле: она складывается целиком и открывается
готовой ссылкой. В остальных магазинах подобранное показывается в окне заказа с
ценами, а оформить его можно только самому. Если выгоднее всего вышло там, где
корзину Ноа не собирает, он назовёт цену и предложит собрать там, где умеет:
«скажите „во ВкусВилле“, и соберу там».

Товар выбирается среди подходящих по названию и по цене за литр или
килограмм: «пять пачек полосатых семечек» — это пять пачек, и именно
полосатых. Оптовые упаковки — канистра на пять литров, лоток на сотни яиц — в
домашнюю корзину не попадают. Уточнения понимаются в разговоре: «а семечки
возьми полосатые», «собери во ВкусВилле». Покупка — это заказ, а не дело:
«купи яйца и хлеб» в список задач не попадает.

Оплата без вашего участия включается в разделе «Заказы» окна настройки и
работает только в пределах — по умолчанию до 3000 ₽ за заказ и до 5000 ₽ за
сутки. Всё, что больше, ждёт кнопки в окне заказа. Там же — какие магазины
участвуют, вход в аккаунт магазина, ключ parse.bot и адрес сервиса FoodPilot,
через который идут поиск и корзина. «Ноа, открой настройки заказов» открывает
этот раздел голосом. Номер карты и пароли Суфлёр не хранит: вход идёт на
странице самого магазина, карта остаётся в аккаунте магазина.

## Настройки

Из коробки объяснения даёт своя модель на этом же компьютере: без ключей, без
регистрации и без интернета, и ничего никуда не отправляется. Она же умеет
«простыми словами», примеры и уточняющие вопросы.

Если своя модель не подходит — компьютер слабый, места на диске нет, — в окне
настройки выбирается другой источник: Википедия (без ключа, только определения) или
облачный сервис по ключу. То же самое можно задать файлом `config.json` рядом
с программой:

```json
{
  "ai": {
    "provider": "http",
    "endpoint": "https://адрес-вашего-api",
    "apiKey": "ключ",
    "model": "название-модели",
    "proxy": ""
  },
  "ui": {
    "theme": "system"
  }
}
```

`theme` — `system`, `dark` или `light`. Остальные параметры (таймауты, повторы запросов)
описаны в `config.example.json`.

**Если сервис не отвечает.** Часть сервисов моделей недоступна из некоторых стран: ключ
есть, интернет есть, а запрос не доходит. Впишите адрес прокси в поле «Прокси» в окне
настройки или в `proxy` в файле — понимаются `http://хост:порт` и `socks5://хост:порт`.
VPN-клиенты обычно поднимают socks5 на `127.0.0.1`, порт смотрите в их настройках.
Википедия работает без этого.

<img src="docs/light.png" width="520" alt="Тот же попап в светлой теме">

## Что стоит знать

**Память под модель.** Своя модель занимает видеопамять, пока ею пользуются:
после последнего вопроса она держится в памяти двадцать минут, а при выходе из
Суфлёра выгружается сразу. Когда Ноа зовут по имени или зажимают левый Alt с
пробелом, модель поднимается заранее — пока вы договариваете вопрос. Ollama —
отдельная программа: её собственный процесс (`ollama.exe`) после выхода остаётся
работать, но без модели почти ничего не занимает.

**Буфер обмена на Windows.** Программы на Chromium — Chrome, Edge, Telegram, Discord,
VS Code — не показывают системе выделенный текст. Для них Суфлёр нажимает Ctrl+C за вас
и сразу возвращает в буфер обмена то, что там было. Способ включён по умолчанию и
выключается галочкой в окне настройки.

## Сборка из исходников

```bash
npm install
npm run tauri dev      # запуск
npm run tauri build    # установщик для текущей системы
```

Нужен [Rust](https://rustup.rs) и Node 20+. На Linux дополнительно:

```bash
sudo apt install libwebkit2gtk-4.1-dev libgtk-3-dev \
  libayatana-appindicator3-dev librsvg2-dev libssl-dev
```


