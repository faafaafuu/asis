{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Права для окон попапа, мини-меню, онбординга и индикатора голоса. Изменение размера/позиции делает Rust, поэтому окнам не выдаются window-права на запись. Окно без прав не получает ни событий, ни ответов на вызовы — молча и без ошибок, поэтому каждое новое окно надо перечислять здесь.",
  "windows": ["popup", "menu", "onboarding", "hud"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging"
  ]
}
